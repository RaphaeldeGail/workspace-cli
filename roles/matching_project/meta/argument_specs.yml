---
argument_specs:
  main:
    short_description: Fetch a GCP organization info from its domain name.
    description:
      - Fetch GCP info about an organization from its domain name.
      - The organization name and directory customer ID are retrieved.
    author:
      - Raphaël de Gail
    options:
      root_project:
        type: 'dict'
        required: true
        description:
          - The definition of the root project.
        options:
          base_name:
            type: 'str'
            required: true
            description:
              - The user-friendly name of the project.
          labels:
            type: 'dict'
            required: true
            descrption:
              - A key/value map characterizing the project.
 